const userTasks = [];
const todayTasks = [];
const weekTasks = [];
const monthTasks = [];

class Task {
    constructor(title, descritpion, date) {
        this.title = title;
        this.descritpion = descritpion;
        this.date = date;
    }
}

const adddefaultTask = (title, desc, date) => {

    
};

const adduserTask = () => {

};